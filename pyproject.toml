[project]
name = "caddie-llm"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "openai>=3.2.0",
    "pgvector>=0.5.0",
    "pypdf>=6.16.1",
]


[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.fastapi]
entrypoint = "main:app"

